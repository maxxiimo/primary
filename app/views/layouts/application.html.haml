!!!
%html.no-js{lang: 'en'}
  = render 'layouts/head'
  %body
    #wrapper.sticky
      = render 'layouts/browsehappy'
      %header{role: 'banner'}
        .container
          = render 'layouts/logo'
          = yield :main_navigation
      #main{role: 'main'}
        .container
          = yield
      = render 'layouts/footer'
    = render 'layouts/scripts'
