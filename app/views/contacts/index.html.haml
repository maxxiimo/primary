- content_for :title do
  Contact
= nav_at 'Contact Us'

%p We're looking forward to meeting you!
%p.note Contact form error messages and alerts will need to be designed. Send message to experience interaction.
= render 'shared/phone_number'
= render 'shared/email_encryptor'
= render 'shared/social_links'
= render 'contact_form'
= render 'shared/snail_mail'
= render 'shared/map'
